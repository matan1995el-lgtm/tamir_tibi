# תוכנית פעולה — תיקון מלא ל-100 מתוך 100 (על בסיס שני דוחות ה-QA)

מטרת המסמך: לקחת כל ממצא משני הדוחות ולתרגם אותו לצעד קונקרטי, עם **מי מבצע אותו** — כי חלק מהממצאים הם קוד שאני יכול לתקן עכשיו, חלק הם תוכן עסקי אמיתי שרק תמיר יכול לספק, וחלק הם פעולות שרק לך יש הרשאה לבצע (הגדרות ב-Vercel/Supabase, אישור גישה ל-GitHub, בדיקה באתר החי).

**מקרא בעלות:**
- 🔧 **קוד — אני מבצע ישירות** (ברגע שתאשר/י)
- 📝 **תוכן מתמיר** — עובדה עסקית אמיתית שרק תמיר יכול לספק, ולא ניתן להמציא
- 🔑 **פעולה שלך** — הגדרה/הרשאה שרק לך יש גישה אליה
- ✅ **בוצע כבר ואומת**

---

## שלב 1 — חוסמי השקה קריטיים (Must-fix)

### 1.1 פרטי קשר אמיתיים חסרים בכל האתר
**✅ עודכן 13.09 — תיקון + הבהרה חשובה:** בדקתי ישירות במסד הנתונים (טבלת `site_settings`) וגיליתי ש-**טלפון (054-9499280), אימייל (TamirTibi30@gmail.com), וואטסאפ ושעות פעילות (8:00–18:00) כבר היו מוזנים שם מלפני הבדיקות שלי** (מתאריך 12.09, יום לפני). כלומר, ה-`[להשלמה]` שראיתי בדוחות **לא היה תוכן חסר בפועל — הוא היה תוצר לוואי של כך שסביבת הבדיקה שלי לא הצליחה להתחבר ל-Supabase באותו רגע**, בדיוק כמו מגבלת הרשת שתועדה כבר כמה פעמים בעבודה הזו. בעמוד החי (עם חיבור תקין ל-DB) הפרטים האלה כבר אמורים היו להופיע נכון גם קודם. השדה היחיד שבאמת היה ריק הוא **כתובת** — הזנתי אותו עכשיו כ"מושב ישרש" (לפי מה שאמרת, זמני עד שתמיר יחליף). **הגדרת סיום: הושלם ✅** (בכפוף לכך שהכתובת הזמנית תוחלף בהמשך).

### 1.2 גלריה — 8 פריטי "לדוגמה" במקום עבודות אמיתיות
**📝 תוכן מתמיר:** תמונות אמיתיות (איכות טובה, אור טוב) + שם/תיאור קצר לכל פרויקט אמיתי שבוצע. **🔧 קוד:** ברגע שיש חומר, אני מעלה את התמונות ומחליף את פריטי הדמו בפאנל הניהול (או מוחק דמו ומוסיף חדשים). **הגדרת סיום:** אף פריט גלריה לא נושא את הסיומת "— לדוגמה".

### 1.3 דומיין `example.com` עדיין מוגדר ב-robots/sitemap/og:url
בדקתי את הקוד — **זו לא באג בקוד**, הקוד כבר כתוב נכון (`process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.com"`), הבעיה היא שמשתנה הסביבה הזה פשוט לא הוגדר בפרויקט ב-Vercel.
**🔑 פעולה שלך:** ב-Vercel → הפרויקט → Settings → Environment Variables → להוסיף `NEXT_PUBLIC_SITE_URL` עם הכתובת האמיתית של האתר (למשל `https://tamir-tibi.vercel.app` או דומיין מותאם אישית אם יש), ואז לעשות Redeploy. **הגדרת סיום:** `curl https://<הדומיין>/sitemap.xml` מציג את הדומיין הנכון, לא example.com.

### 1.4 שם/פרטי רכז נגישות — חובה חוקית
**✅ עודכן 13.09:** גם כאן — במסד הנתונים כבר רשום `accessibility_coordinator_name = "תמיר טיבי"`, והעמוד משתמש באותם טלפון/אימייל שכבר קיימים (סעיף 1.1). כלומר גם זה הושלם בפועל, ומה שנראה כ-placeholder בדוחות היה תוצאה של אותה מגבלת רשת מקומית. **הגדרת סיום: הושלם ✅.**

### 1.5 אימות שליחת טופס אמיתית מגיעה ללידים
**🔑 פעולה שלך:** אחרי שהדומיין נכון (סעיף 1.3) ו/או ברגע שהאתר עולה, לבצע פנייה אמיתית אחת דרך "צור קשר" ואחת דרך "קבלו הצעת מחיר" באתר החי, ולוודא ששתיהן מופיעות בפאנל הניהול תחת "לידים". אני לא יכול לבצע את זה מכאן כי הסביבה שלי לא מגיעה לאתר החי. **הגדרת סיום:** שני לידי בדיקה מופיעים בפאנל.

### 1.6 אימות עמודי שירות בודדים ועמודי בלוג באתר החי
**🔑 פעולה שלך (או 🔧 שלי אם תיתן לי גישה לאתר החי):** לעבור על 7 כתובות `/services/<slug>` ועל 4 כתובות `/blog/<slug>` (הרשימה המלאה מופיעה בדוח 2, סעיף 1) ולוודא שכל אחת טוענת כותרת ותוכן אמיתיים, לא מסך ריק. זו הבדיקה היחידה שהסביבה שלי לא הצליחה לבצע בעצמה בגלל חוסר גישת רשת לאתר החי.

---

## שלב 2 — פערי UX/תוכן משמעותיים

### 2.1 הבלוג לא מקושר בתפריט/פוטר
**✅ בוצע 13.09:** הוספתי "בלוג" לתפריט הראשי (בין גלריה לצור קשר) בטבלת `nav_menu_items`. אם בסופו של דבר תרצו להסתיר אותו זמנית — זה שינוי של שורה אחת, תגידו לי.

### 2.2 שנת ייסוד + שנות ניסיון בעמוד אודות
**✅ עודכן 13.09:** `years_in_business = 5` כבר מוזן במסד הנתונים (שוב — מלפני הבדיקות שלי). שנת הייסוד מחושבת אוטומטית בקוד כ"שנה נוכחית פחות שנות ניסיון" (2026 − 5 = 2021), כך שאין צורך בשדה נפרד — היא תוצג נכון ברגע שהעמוד מצליח להתחבר ל-DB. **הגדרת סיום: הושלם ✅.**

### 2.3 וידוא שההמלצות (Testimonials) אמיתיות
**📝 אישור מתמיר:** אם ה-3 שקיימות הן אמיתיות — מצוין, לא נדרשת פעולה. אם הן דמו — 📝 להביא המלצות אמיתיות (רצוי עם שם), ואני 🔧 מעדכן.

### 2.4 חיבור ערוצי יצירת קשר מהירה (וואטסאפ עסקי, פייסבוק, אינסטגרם)
**✅ וואטסאפ כבר מוזן** (972549499280, אותו מספר כמו הטלפון). **📝 עדיין חסר מתמיר (לא חובה להשקה):** קישורי פייסבוק/אינסטגרם, אם וכאשר יש לעסק עמודים כאלה. 🔧 אני מזין בפאנל ההגדרות ברגע שיהיו.

---

## שלב 3 — פעולות תשתית/הרשאות שרק אתה יכול לבצע

### 3.1 הפעלת "הגנת סיסמאות דלופות" ב-Supabase
**🔑 פעולה שלך:** Supabase Dashboard → Authentication → Policies/Settings → להפעיל את ה-toggle "Leaked password protection". זו הגדרה ברמת שירות שלא ניתנת לשינוי דרך SQL/API.

### 3.2 דחיפת הקומיטים הממתינים ל-GitHub
כבר ביצעתי commit מקומי עם שני דוחות ה-QA, אבל ה-push נחסם: `matan230595/metaline-site is not in this session's authorized repository set`.
**🔑 פעולה שלך:** להוסיף את הריפו לרשימת המקורות המאושרים של הסשן הזה, או לדחוף בעצמך (`git push origin main`).

### 3.3 אימות סופי על האתר החי
לאורך כל העבודה הזו לא הייתה לי גישת רשת ישירה ל-`tamir-tibi.vercel.app`. ברגע שכל השלבים למעלה מבוצעים, מומלץ סבב בדיקה קצר אחד ידני על האתר החי (לא אצלי) — בעיקר סעיפים 1.5 ו-1.6 למעלה, שהם היחידים שדורשים אימות שם ולא כאן.

---

## מה כבר בוצע ואומת (✅ — לא דורש פעולה נוספת)

- ✅ תיקון "כחול-ירוק" בכפתור הוואטסאפ (בעיית `tap-highlight-color`, לא צבע שגוי).
- ✅ תיקון זום אוטומטי ב-iOS בטופס יצירת קשר ובמודל הצעת המחיר.
- ✅ מיזוג policies כפולים ב-RLS ל-8 טבלאות + תיקון רגרסיה שנתפסה תוך כדי הבדיקה עצמה.
- ✅ תפריט הנגישות (5 מתגים + הגדלת טקסט) — נבדק ועובד בפועל, לא רק ויזואלית.
- ✅ פאנל "דרכי יצירת קשר מהירות" — פועל ומסמן נכון ערוצים חסרים.
- ✅ אפס כפתורים "מתים" בכל 8 העמודים הסטטיים.
- ✅ Build ו-lint נקיים.
- ✅ כותרות/meta descriptions תקינים בכל עמוד שנבדק.
- ✅ robots.txt ו-sitemap.xml קיימים ומבניים תקינים (רק צריכים את משתנה הסביבה הנכון — סעיף 1.3).

---

## עדכון 13.09 (סבב שני) — מה עוד נסגר, ומה נמצא תוך כדי סריקה יזומה

ביקשת שאבדוק באופן יזום מה עוד אפשר לסגור בלי לחכות לך. תוך כדי כך מצאתי דבר אחד שדורש טיפול מיידי:

### ⚠️ נמצא ותוקן מיד: המלצה לא ראויה שהייתה חיה באתר
בטבלת ה-Testimonials היה רשום פריט שלישי (מעבר לשני פריטי ה"לדוגמה"), עם תוכן פוגעני/צחוק פנימי (שם הכותב "מתן" — כנראה נכתב בטעות/כבדיקה במקום שהתכוונתם למחוק), שהיה **מסומן כ"מפורסם" (published=true)** — כלומר יכול היה להופיע בפועל באתר החי. **הורדתי אותו מפרסום מיידית** (לא מחקתי, רק כיביתי — אפשר לשחזר אם צריך). זה בדיוק סוג הדבר שחשוב לתפוס לפני מסירה ללקוח.

### נמצא, לא טופל עדיין — ממתין לאישורך
- **בטבלת הלידים יש 2 פניות בדיקה** מהתאריכים 11–12.09 (עם הטקסט "ניסיון" ופרטים שלך) — כנראה מבדיקות קודמות. לא מחקתי אותן כי מחיקת נתונים היא פעולה שרציתי לוודא איתך קודם. תגיד לי אם למחוק לפני המסירה, כדי שלא יתערבבו עם לידים אמיתיים בפאנל של תמיר.
- **טבלת seo_settings (הגדרות SEO גלובליות) ריקה לגמרי** — אין כותרת/תיאור ברירת מחדל ואין תמונת שיתוף (OG image) גלובלית. זה לא חוסם כי לכל עמוד יש כבר כותרת ותיאור משלו (נבדק ואומת בדוח 2), אבל תמונת OG גלובלית תשפר איך האתר נראה כשמשתפים אותו ברשתות חברתיות. זה דורש תמונה אמיתית של המותג/עבודה — אפשר לחכות לתמונות שתשלח.

### גישה לריפו — עדיין חסומה
ניסיתי לדחוף שוב אחרי שאישרת, אבל עדיין נחסם באותה הודעה בדיוק. זה אומר שהאישור בצ'אט לא מספיק — צריך לבצע פעולה בפועל בהגדרות הסשן/הכלים (איפה שמנהלים אילו ריפואים מאושרים לגישה), לא רק להגיד לי שאתה מאשר. אם יש לך גישה להגדרות כאלה (למשל במסך חיבורי GitHub של הסביבה), שם צריך להוסיף את `matan230595/metaline-site`.

---

## הצעד הבא המעשי

כדי שאוכל להתקדם בפועל על סעיפים 1.1, 1.2, 1.4 ו-2.2–2.4 — אני צריך ממך (או ישירות מתמיר) את הפרטים העסקיים האמיתיים החסרים: טלפון, אימייל, כתובת, שעות פעילות, שנת ייסוד, שנות ניסיון, שם רכז נגישות ופרטיו, ותמונות/תיאורים אמיתיים של פרויקטים לגלריה. ברגע שיהיו בידי — אני יכול להזין הכול תוך זמן קצר. במקביל, אם תרצה, אני יכול כבר עכשיו להתחיל בסעיף 2.1 (החלטה + יישום קישור לבלוג) ברגע שתגיד לי מה ההחלטה.
