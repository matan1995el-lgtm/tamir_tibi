# תוכנית עבודה שלב־אחר־שלב — כיסוי 37 ההערות + תקלת הגלריה

מקור: התקבלה ממתן בצ'אט ב-14.09.2026, כתוכנית מסודרת ליישום ממצאי דוח הביקורת (Panda Digital Agency) + תקלת opacity שהתגלתה בגלריה.

*(התוכן המלא של 13 השלבים כפי שנשלח בצ'אט — ראו את הודעת המשתמש בתאריך 14.09.2026 להעתק המקורי. המסמך הזה משמש עוגן הפניה; ההתקדמות מתועדת מול `2026-09-13-action-plan-100.md` באותה תיקייה.)*

## הצלבה מול מה שכבר בוצע בפועל (עד 14.09.2026)

- שלב 2 (גלריה): התוכן (כותרות אמיתיות, מיפוי 10 תמונות) הוכן במסד הנתונים; העלאת קובצי התמונה עצמם ממתינה לביצוע ידני של תמיר/מתן דרך פאנל הניהול (Storage לא נגיש מסביבת הפיתוח הנוכחית). תקלת ה-`opacity: 0` בכרטיסים **לא נבדקה עדיין בקוד בסבב הזה** — צריכה בדיקה.
- שלב 5 (SEO/canonical/sitemap): `NEXT_PUBLIC_SITE_URL` עדיין לא מוגדר ב-Vercel (חוסם את כל הסעיף) — פעולה אצל מתן, הודרך במדריך נפרד.
- שלב 6 (נתונים מובנים): `default_meta_title`/`default_meta_description` מולאו ב-`seo_settings`; JSON-LD מובנה (LocalBusiness/Article/Breadcrumb) **טרם נוסף בקוד**.
- שלב 7 (אימות תוכן): המלצות לדוגמה — **הושארו במפורש לפי הנחיית מתן, לא לטפל כרגע**. שאר פרטי העסק (שנת ייסוד, ניסיון, מספר פרויקטים) כבר מאושרים ומוזנים.
- שלב 10 (מדיניות/כותרות אבטחה): `X-Content-Type-Options`, `Referrer-Policy`, CSP — טרם נבדקו/נוספו בסבב הזה.
- שלב 3, 4, 8, 9, 11, 12: טרם נבדקו/בוצעו בסבב הזה — דורשים עבודת קוד (git) שתלויה בפתרון חסימת הגישה ל-git push, או בביצוע ישיר של מתן/מפתח לפי המדריך שנשלח.

## הערת יסוד

חלק ניכר מהשלבים (3, 6, 8, 9, 10, 12) דורשים עריכת קוד ו-git commit — והדחיפה בפועל ל-GitHub עדיין תלויה בפעולת ההעלאה הידנית שהודרכה בנפרד (כל עוד היא לא בוצעה, כל שינוי קוד חדש שייווצר כאן יצטרף לאותה ערימת "קבצים מוכנים, ממתינים להעלאה ידנית").
