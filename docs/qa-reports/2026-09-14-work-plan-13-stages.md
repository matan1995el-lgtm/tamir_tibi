# תוכנית עבודה שלב־אחר־שלב — כיסוי 37 ההערות + תקלת הגלריה

מקור: התקבלה ממתן בצ'אט ב-14.09.2026, ליישום ממצאי דוח ביקורת חיצוני (Panda Digital Agency, 37 ממצאים) + תקלת opacity שהתגלתה בגלריה.

סדר עדיפות שהוגדר: **גלריה → טפסים → SEO → נגישות → תוכן → עיצוב ומובייל → מדיניות → בדיקות מסירה וביצועים**.

## מצב סופי לפי שלב (סבב שני, הושלם 14.09.2026)

כל מה שמסומן ✅ בוצע בקוד, נבדק (`tsc`+`eslint`+`next build` נקיים) ונשמר כ-commit נפרד. כל מה שמסומן ⏳ דורש תשובה עסקית/פעולת הרשאה מכם או מתמיר — ראו את השאלון שנשלח בנפרד.

- **שלב 1 (הכנת סביבה):** ✅ git + commit נפרד לכל קבוצת תיקונים. ⏳ אין preview deployment חי (לא זמין מהסביבה הזו).
- **שלב 2 (גלריה):** ✅ הושלם במלואו — תוקן opacity:0 (אומת שוב), fallback לתמונה שנכשלת בטעינה, lightbox להגדלה בלחיצה, ניסוח דינמי שלא טוען "פרויקטים שביצענו" כל עוד יש הדמיות (מתקן את עצמו אוטומטית ברגע שכל הפריטים יקבלו תמונה אמיתית). ⏳ התמונות עצמן — ממתין להעלאה שלכם דרך פאנל הניהול.
- **שלב 3 (טפסים):** ✅ הושלם במלואו — אימות טלפון בצד לקוח+שרת עם נרמול ל-+972, מניעת ליד כפול (idempotency key + מיגרציית DB), timeout שמונע "שולח..." תקוע, מזהה שירות קבוע (slug) במקום התאמה לפי כותרת עברית שניתנת לשינוי, שדה יישוב/אזור חדש, מילוי אוטומטי בדפדפן.
- **שלב 4 (קשר וניווט):** ✅ הושלם במלואו — tel:/mailto: אמיתיים, קישורי שירותים בפוטר לעמוד הנכון, אייקון כפתור צף מתוקן, תפריט מובייל נבדק כבר תקין.
- **שלב 5 (URL ושיתוף):** ✅ 5.1–5.7 הושלמו — כל עמוד קיבל canonical עצמאי (תוקן ב-11 קבצים; קודם כל עמוד הצהיר על עמוד הבית), og:url לעמודים דינמיים. ⏳ 5.8/5.9 (תמונת שיתוף) — ממתין לנכס אמיתי.
- **שלב 6 (נתונים מובנים):** ✅ הושלם במלואו — JSON-LD מסוג LocalBusiness (מה-layout), Service לכל עמוד שירות, Article לכל פוסט בלוג, BreadcrumbList לשניהם. הכול בנוי רק משדות אמיתיים קיימים.
- **שלב 7 (אימות תוכן מול תמיר):** ⏳ **לא בוצע בכוונה** — דורש תשובות עסקיות אמיתיות (ראו השאלון).
- **שלב 8 (נגישות מבנית):** ✅ הושלם במלואו — focus trap אמיתי במודל הצעת מחיר ובתפריט הנגישות, main landmark + קישור דילוג לתוכן, שם נגיש (aria-label) לכל אחד מ-5 מתגי הנגישות, תיקון סדר כותרות (h1→h4 בעמוד צור קשר, h2→h4 בכרטיסי תהליך/השוואה — היו נבחרות לפי גודל פונט, לא מבנה).
- **שלב 9 (עיצוב ומובייל):** ⏳ לא בוצע בקוד — נבדק שהמנגנון הקיים (כיבוד prefers-reduced-motion, מרווח דינמי של הכפתורים הצפים מעל סרגל ה-CTA הנייד) כבר תקין ולא דרש שינוי. שאר הסעיפים (מרווחים/אנימציות מוגזמות, התאמה למקלדת נייד) דורשים בדיקה חזותית על מכשיר אמיתי — לא ניתן לאמת מהסביבה הזו בלי screenshot/browser חי.
- **שלב 10 (מדיניות ואבטחה):** ✅ 10.2/10.6/10.7 בוצעו — תוקן תיאור שגוי (העדפות נגישות נשמרות ב-localStorage, לא בעוגייה — אומת בקוד), כותרות אבטחה (X-Content-Type-Options, Referrer-Policy, X-Frame-Options, Permissions-Policy), CSP במצב ניטור-בלבד (Report-Only) בהתאם למה שהאתר באמת טוען. ✅ 10.8 (HSTS) — נשאר כפי שהוא לפי ההנחיה המפורשת בתוכנית, זו הגדרת פלטפורמת Vercel לא קוד. ⏳ 10.1/10.3/10.4/10.5 (רשימת מידע שנאסף, כלי מדידה בפועל, זהות מפעיל האתר, פרטי קשר במסמכים) — דורש תשובות עסקיות (ראו השאלון).
- **שלבים 11–13 (בדיקת קצה־לקצה, ביצועים, סגירה):** ⏳ לא ניתן לבצע מהסביבה הזו — דורשים גישה לאתר החי. נשארים "דורש אימות" במפורש.

## מה נשאר פתוח ומחכה לכם

כל הפריטים שסומנו ⏳ למעלה מפורטים ומנוסחים כשאלות קונקרטיות בהודעת הצ'אט שנשלחה יחד עם המסמך הזה.

## הערת יסוד

כל שינויי הקוד בסבב הזה (12 commits) הם **local-only** — הדחיפה בפועל ל-GitHub תלויה בהעלאה הידנית. ZIP מעודכן נשלח בסוף הסבב.
